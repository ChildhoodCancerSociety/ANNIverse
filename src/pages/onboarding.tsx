// https://headlessui.com/react/transition
import { Transition } from "@headlessui/react";

const Onboarding: React.FC = () => {
  /**
   * loading state
   *
   * success state
   * - some click-to-copy invite link/ open in-app link (not sure what form this would take)
   * - confetti
   *
   * need more info state
   * - email input
   */
  return null;
};

export default Onboarding;
